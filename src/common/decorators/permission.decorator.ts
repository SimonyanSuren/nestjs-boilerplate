//import { type Resource } from '@common/@types/enums';
//import { SET_PERMISSIONS_KEY } from '@common/constants';
//import { type CustomDecorator, SetMetadata } from '@nestjs/common';

//export const SetPermissions = (
//  recourse: Resource,
//  conditionKey?: string,
//): CustomDecorator<string> => SetMetadata(SET_PERMISSIONS_KEY, [recourse, conditionKey]);
